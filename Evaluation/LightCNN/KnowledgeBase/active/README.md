# Active LightCNN knowledge base

`lightcnn_evidence.json` is the compact knowledge base consumed by
the Gen-TAS allocation prompt builder. It contains task characterizations and
the eight measured three-stage partition profiles, including communication and
hardware measurements where available.
